export class Rotation {
    /**
     * Creates an instance of Rotation.
     * @param {number} amount 
     * @param {number} speed 
     */
    constructor(amount, speed) {
        this.amount = amount;
        this.speed = speed;
    }
}